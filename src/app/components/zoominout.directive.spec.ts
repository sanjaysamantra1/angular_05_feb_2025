import { ZoominoutDirective } from './zoominout.directive';

describe('ZoominoutDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoominoutDirective();
    expect(directive).toBeTruthy();
  });
});
